import {
    absolute,
    inherit
} from '../../../../../utils/themer'

export const mobileMenuItemIconStyle = {
    mobile: {
        position: absolute,
        size: 22,
        right: 25,
        color: inherit
    }
}